# 서비스 정의: 브라우저 트래픽 분석 Agent

브라우저 개발자 도구(DevTools)와 유사하게 동작하며, 브라우저 네트워크 트래픽 데이터 중
사용자가 정의한 특정 패턴(정규식)이 존재하는지 탐지하는 온디맨드 점검 도구.

## 목적/범위

- 개인 웹 브라우징 중, 필요할 때만 실행하는 온디맨드 점검 도구 (상시 백그라운드 감시 아님)
- 대상 브라우저: Chrome/Chromium 계열만

## 현재 방식 (Before)

- 사람이 브라우저 개발자 도구(DevTools)를 직접 열어 네트워크/콘솔 데이터를 수동으로 확인
- 웹사이트 특성상 변경이 자주 발생하는데, 수동 확인이라 주기적으로 반복하기 어려워 변경을 놓치기 쉬움

## 후순위 (이번 범위 밖, 다음 단계에서 고려)

- **방문 URL 자동 수집**: 사용자가 실제로 방문하는 URL을 약 10초간 모니터링해 자동으로 `data/urls.txt`에 추가하는 기능. 지금은 `urls.txt`를 사용자가 수동으로 채워야 한다.
- **일일 리포팅**: 하루 동안의 스캔 결과를 정기적으로 요약해 보여주는 기능. 지금은 필요할 때마다(온디맨드) `query_matches`로 직접 조회해야 한다.

## 수집 방식

- Playwright(Python)로 CDP(Chrome DevTools Protocol) 후킹
- 새 브라우저 세션을 열어 미리 저장된 URL목록 파일을 순차적으로 trace 수행
- 개별 페이지 방문 실패 시 해당 페이지만 건너뛰고 크롤링 계속

## 수집 데이터

- 네트워크 요청/응답 (헤더, 바디, 쿠키 포함)
- 콘솔 로그 / JS 에러

## 패턴 탐지

- 사용자가 정의한 정규식 패턴 사용
- config 파일에 `{ name, regex }` 목록 형태로 등록/관리

## 매칭 시 기록 항목

- 매칭된 값 전체를 원본 그대로 저장
- 메타데이터
  - URL / 요청 출처
  - 타임스탬프
  - 위치 (헤더 / 바디 / 쿠키 / 콘솔 중 어디서 매칭되었는지)
  - 매칭된 패턴 이름 (정규식 이름)

## 저장/조회

- 로컬 SQLite DB에 저장
- 조회 가능한 형태로 관리

## 알림

- 스캔 중 매칭 발생 시 터미널/콘솔에 즉시 출력 (도구 실행 중 부수 효과, LLM 응답과 별개)

## 에이전트 역할

- `langchain.agents`의 `create_agent`로 생성한 LLM 에이전트 (모델: Amazon Bedrock `ChatBedrockConverse`)
- 수집→탐지→저장은 도구(tool) 내부에 **고정된 순서**로 구현되어 있으며, 에이전트(LLM)가 그 세부 순서를 즉흥적으로 정하지 않음
- 에이전트가 담당하는 부분
  - 사용자의 자연어 요청을 보고 `run_scan`(점검 실행) / `query_matches`(기존 결과 조회) 중 어떤 도구를 호출할지 판단
  - 도구 실행 결과(매칭 건수, 패턴별/위치별 분포 등)를 사람이 이해하기 쉬운 자연어로 요약

## 성공 기준

다음을 모두 만족하면 이 서비스가 목적한 대로 동작하는 것으로 본다.

1. **단발 실행**: `python -m src.main "<자연어 요청>"` 실행 시 별도 서버를 띄우지 않고, 한 번의 요청 → 응답으로 프로세스가 완결된다
2. **도구 선택 정확성**: 요청 의도(새로 점검 vs 기존 결과 조회)에 맞는 도구(`run_scan`/`query_matches`)가 호출된다
3. **탐지 정확도**: config에 등록된 패턴이 실제로 존재하는 데이터에서는 누락 없이 탐지되고(거짓 음성 없음), 존재하지 않는 데이터에서는 매칭이 발생하지 않는다(거짓 양성 없음)
4. **내결함성**: 일부 URL 방문이 실패해도 나머지 URL 점검은 계속되며, 전체 스캔이 중단되지 않는다
5. **데이터 무결성**: 매칭된 값과 메타데이터(URL/타임스탬프/위치/패턴명)가 원본 그대로 SQLite에 저장되고, 이후 조회 결과가 저장된 값과 정확히 일치한다
6. **즉시성**: 매칭 발생 시점과 콘솔 알림 출력 시점 사이에 사실상 지연이 없다 (다음 URL 처리 전에 출력됨)
7. **재현성**: 동일한 `patterns.json`+`urls.txt`로 반복 실행해도 동일한 매칭 결과가 산출된다
8. **전체 통과율**: [verification.md](verification.md) 체크리스트의 테스트 케이스 중 **90% 이상 Pass**해야 출시 가능한 것으로 본다

세부 판정 기준은 [verification.md](verification.md) 체크리스트를, 실제 질의 기반 검증은 [design.md](design.md) 9절(`evaluation/`)을 참고한다.
